UniPhy verification

```bash
python -m Check.tests.run_all --tests T S --log-dir <log_dir> --json-out <json_path>
```

Retained tests cover numerical operators, timestep semantics, recurrence equivalence, regression behavior, and structural contracts used to keep the mathematical implementation coherent.
